import { Body, Header, Navbar, Title } from "../components/utils";

// Pagina per gli ospiti
const Pro = () => {
  return (
    <Body>
      <Navbar page="PRO" />
      <Header>
        <Title>Go pro</Title>
      </Header>
    </Body>
  );
};

export default Pro;
