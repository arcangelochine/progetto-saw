import { styled } from "styled-components";
import { Center, Row } from "../utils";

export const Container = styled(Center)`
  margin-top: auto;
  gap: 10px;
`;

export const LogoAndTitle = styled(Row)`
  gap: 4px;
  align-items: center;
`;
